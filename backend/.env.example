# Supabase project connection (backend / server-side only)
SUPABASE_URL=https://iotbekdizucalvjqoebr.supabase.co

# anon/publishable key - used only to validate user JWTs against GoTrue
SUPABASE_ANON_KEY=sb_publishable_P0DuaWdx92IYDJKezZkBSA__oW1cHPM

# SERVICE ROLE KEY - SECRET. Bypasses RLS. Never expose to the browser.
# Copy from: Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: default LLM provider key used when a user doesn't supply their own.
OPENAI_API_KEY=
